const STUDENTS = [{ "name": "Aditi Chaurasia", "roll_number": 2613001, "email_id": "aditichaurasia.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "Amalekh Santhosh", "roll_number": 2613002, "email_id": "amalekhsanthosh.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "Amrit Dutta", "roll_number": 2613003, "email_id": "amritdutta.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "Ananya Tunga", "roll_number": 2613004, "email_id": "ananyatunga.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "Aniket Jain", "roll_number": 2613005, "email_id": "aniketjain.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "Anirudh Kumar", "roll_number": 2613006, "email_id": "anirudhkumar.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "Ankit Gupta", "roll_number": 2613007, "email_id": "ankitgupta.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "Anubhav Mishra", "roll_number": 2613008, "email_id": "anubhavmishra.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "Apil Kakkar", "roll_number": 2613009, "email_id": "apilkakkar.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "Apoorva Singhal", "roll_number": 2613010, "email_id": "apoorvasinghal.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "Ashish Ranjan", "roll_number": 2613011, "email_id": "ashishranjan.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "Avik Sinha", "roll_number": 2613012, "email_id": "aviksinha.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "B Vinay Rao", "roll_number": 2613013, "email_id": "bvinayrao.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "Dhyeykumar Rathod", "roll_number": 2613015, "email_id": "dhyeykumarrathod.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "Divya Das", "roll_number": 2613016, "email_id": "divyadas.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "Divyansh Sengar", "roll_number": 2613017, "email_id": "divyanshsengar.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "Gaurav .", "roll_number": 2613018, "email_id": "gaurav.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "Guduru Sai Sandeep", "roll_number": 2613019, "email_id": "gudurusaisandeep.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "Harshita Tiwari", "roll_number": 2613020, "email_id": "harshitatiwari.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "Himanshi Sirwani", "roll_number": 2613021, "email_id": "himanshisirwani.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "Kevin Mathews", "roll_number": 2613022, "email_id": "kevinmathews.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "Kriti Grover", "roll_number": 2613023, "email_id": "kritigrover.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "Mahendra Gaikar", "roll_number": 2613024, "email_id": "mahendragaikar.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "Marikrishnan M", "roll_number": 2613025, "email_id": "marikrishnanm.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "Miti Mehta", "roll_number": 2613026, "email_id": "mitimehta.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "Mrigakshi Das", "roll_number": 2613027, "email_id": "mrigakshidas.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "Neha Ranjeet Sah", "roll_number": 2613028, "email_id": "neharanjeetsah.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "Nishant Sood", "roll_number": 2613029, "email_id": "nishantsood.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "P Sai Krishna", "roll_number": 2613030, "email_id": "psaikrishna.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "Panav Pratap Singh Tyagi", "roll_number": 2613031, "email_id": "panavpratapsinghtyagi.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "Pranav Nahar", "roll_number": 2613032, "email_id": "pranavnahar.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "Pranav Pawar", "roll_number": 2613033, "email_id": "pranavpawar.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "Preeti Sehrawat", "roll_number": 2613034, "email_id": "preetisehrawat.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "Priyadarshini Bhatnagar", "roll_number": 2613035, "email_id": "priyadarshinibhatnagar.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "Purvi Sinha", "roll_number": 2613036, "email_id": "purvisinha.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "Rajkumar S P", "roll_number": 2613037, "email_id": "rajkumarsp.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "Ritik Jain", "roll_number": 2613038, "email_id": "ritikjain.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "Ritik Soni", "roll_number": 2613039, "email_id": "ritiksoni.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "Saimanasa Chadalavada", "roll_number": 2613040, "email_id": "saimanasachadalavada.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "Sathvik Shetty", "roll_number": 2613041, "email_id": "sathvikshetty.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "Shahal Abbas V", "roll_number": 2613042, "email_id": "shahalabbasv.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "Shaurya Singh Jadaun", "roll_number": 2613043, "email_id": "shauryasinghjadaun.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "Shivali Gohad", "roll_number": 2613044, "email_id": "shivaligohad.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "Shivanshu Chaudhary", "roll_number": 2613045, "email_id": "shivanshuchaudhary.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "Shubhi Singh", "roll_number": 2613046, "email_id": "shubhisingh.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "Siddhant Jain", "roll_number": 2613047, "email_id": "siddhantjain.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "Siddharth .", "roll_number": 2613048, "email_id": "siddharth.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "Siddharth Prathap", "roll_number": 2613049, "email_id": "siddharthprathap.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "Somasurya Nanda", "roll_number": 2613050, "email_id": "somasuryananda.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "Sonket Sarkar", "roll_number": 2613051, "email_id": "sonketsarkar.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "Souronil Pal", "roll_number": 2613052, "email_id": "souronilpal.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "Subhapriya Sen", "roll_number": 2613053, "email_id": "subhapriyasen.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "Sudarsan S", "roll_number": 2613054, "email_id": "sudarsans.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "Suhani Khandelwal", "roll_number": 2613055, "email_id": "suhanikhandelwal.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "Tejas Pathrikar", "roll_number": 2613056, "email_id": "tejaspathrikar.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "Utkarsh Tripathi", "roll_number": 2613057, "email_id": "utkarshtripathi.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "Vardaan Pruthi", "roll_number": 2613058, "email_id": "vardaanpruthi.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "Vasudev Mishra", "roll_number": 2613059, "email_id": "vasudevmishra.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "Vibhu Verma", "roll_number": 2613060, "email_id": "vibhuverma.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "Vibhuti Dobhal", "roll_number": 2613061, "email_id": "vibhutidobhal.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "Vidhu Sharma", "roll_number": 2613062, "email_id": "vidhusharma.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "Vishwa Kerkar", "roll_number": 2613063, "email_id": "vishwakerkar.dem2026@iimu.ac.in", "course": "DEM" }, { "name": "Yash Rajwadi", "roll_number": 2613065, "email_id": "yashrajwadi.dem2026@iimu.ac.in", "course": "DEM" }];

// Mirrors mail-templates.json (kept in sync manually — embedded here since
// file:// pages can't fetch() a sibling JSON file due to CORS).
const MAIL_TEMPLATES = {
  "lateEntry": {
    "to": "csa.security@iimu.ac.in",
    "cc": "security@iimu.ac.in",
    "subject": "Request for late entry",
    "body": "Dear Council of Student Affairs,\n\nI hope you are doing well.\n\nThis is to inform you that the following students will be going out today and are expected to return to campus around {{time}}. We kindly request your permission to allow late entry into the campus for them.\n\nList of Students\n{{studentLines}}\n{{vehicleLine}}\nThank you and warm regards."
  },
  "offboarding": {
    "to": "csa.security@iimu.ac.in",
    "cc": "security@iimu.ac.in",
    "subject": "Request for Offboarding",
    "body": "Dear Council of Student Affairs,\n\nI hope you are doing well.\n\nThis is to inform you that I will be offboarding the campus for the following duration.\n\nLeave campus: {{leave}}\nReturn campus: {{back}}\n\nKindly note this and allow permission.\n\nThank you and warm regards."
  },
  "guest-visit": {
    "to": "csa.security@iimu.ac.in",
    "cc": "security@iimu.ac.in",
    "subject": "Request for Guest Visit",
    "body": "Dear Council of Student Affairs,\n\nI hope you are doing well.\n\nThis is to inform that I will have guest(s) visiting the campus on {{visitDate}}, arriving around {{visitStart}} and will leave by {{visitEnd}}.\n\nList of Guests\n{{guestLines}}\n\nKindly note this and allow permission.\n\nThank you and warm regards."
  }
};

function formatTime(value) {
  const [hourStr, minuteStr] = value.split(":");
  const hour24 = Number(hourStr);
  const minute = Number(minuteStr);
  const period = hour24 < 12 ? "AM" : "PM";
  const hour12 = hour24 % 12 === 0 ? 12 : hour24 % 12;
  let label = `${hour12}:${minuteStr} ${period}`;
  if (hour24 === 0 && minute === 0) label += " (Midnight)";
  return label;
}

function formatDateTime(value) {
  const date = new Date(value);
  const datePart = date.toLocaleDateString("en-GB", { day: "2-digit", month: "short", year: "numeric" });
  const timePart = date.toLocaleTimeString("en-US", { hour: "numeric", minute: "2-digit", hour12: true });
  return `${datePart}, ${timePart}`;
}

function formatDate(value) {
  const date = new Date(`${value}T00:00:00`);
  return date.toLocaleDateString("en-GB", { day: "2-digit", month: "short", year: "numeric" });
}

let mode = "lateEntry";

const selected = new Set();

const mailTabs = document.getElementById("mailTabs");
const lateEntryForm = document.getElementById("lateEntryForm");
const offboardingForm = document.getElementById("offboardingForm");
const guestVisitForm = document.getElementById("guestVisitForm");
const studentList = document.getElementById("studentList");
const studentSearch = document.getElementById("studentSearch");
const multiselect = document.getElementById("multiselect");
const multiselectToggle = document.getElementById("multiselectToggle");
const multiselectSummary = document.getElementById("multiselectSummary");
const sendBtn = document.getElementById("sendBtn");
const timeInput = document.getElementById("timeInput");
const vehicleInput = document.getElementById("vehicleInput");
const selectedChips = document.getElementById("selectedChips");
const leaveInput = document.getElementById("leaveInput");
const returnInput = document.getElementById("returnInput");
const visitDateInput = document.getElementById("visitDateInput");
const visitStartInput = document.getElementById("visitStartInput");
const visitEndInput = document.getElementById("visitEndInput");
const guestRows = document.getElementById("guestRows");
const addGuestBtn = document.getElementById("addGuestBtn");

const MODE_FORMS = {
  lateEntry: lateEntryForm,
  offboarding: offboardingForm,
  guestVisit: guestVisitForm
};

mailTabs.querySelectorAll(".tab").forEach(tab => {
  tab.addEventListener("click", () => {
    mode = tab.dataset.mode;
    mailTabs.querySelectorAll(".tab").forEach(t => t.classList.toggle("active", t === tab));
    Object.entries(MODE_FORMS).forEach(([m, form]) => { form.hidden = m !== mode; });
    updateSendState();
  });
});

function renderStudentList(filter) {
  const q = (filter || "").trim().toLowerCase();
  const filtered = STUDENTS.filter(s =>
    s.name.toLowerCase().includes(q) ||
    String(s.roll_number).includes(q) ||
    s.course.toLowerCase().includes(q)
  );

  if (filtered.length === 0) {
    studentList.innerHTML = '<div class="multiselect-empty">No students match your search.</div>';
    return;
  }

  studentList.innerHTML = filtered.map(s => {
    const checked = selected.has(s.roll_number) ? "checked" : "";
    return `
      <label class="multiselect-item">
        <input type="checkbox" data-roll="${s.roll_number}" ${checked}>
        <span>${s.name}</span>
        <span class="roll">${s.roll_number} &middot; ${s.course}</span>
      </label>
    `;
  }).join("");

  studentList.querySelectorAll('input[type="checkbox"]').forEach(cb => {
    cb.addEventListener("change", (e) => {
      const roll = Number(e.target.dataset.roll);
      if (e.target.checked) selected.add(roll);
      else selected.delete(roll);
      updateSummary();
    });
  });
}

function updateSummary() {
  if (selected.size === 0) {
    multiselectSummary.textContent = "Select students…";
  } else if (selected.size === 1) {
    const s = STUDENTS.find(s => selected.has(s.roll_number));
    multiselectSummary.textContent = s.name;
  } else {
    multiselectSummary.textContent = `${selected.size} students selected`;
  }
  renderSelectedChips();
  updateSendState();
}

function renderSelectedChips() {
  const chosen = STUDENTS.filter(s => selected.has(s.roll_number));

  selectedChips.innerHTML = chosen.map(s => `
    <span class="chip">
      ${s.name}
      <button type="button" class="chip-remove" data-roll="${s.roll_number}" aria-label="Remove ${s.name}">×</button>
    </span>
  `).join("");

  selectedChips.querySelectorAll(".chip-remove").forEach(btn => {
    btn.addEventListener("click", () => {
      selected.delete(Number(btn.dataset.roll));
      renderStudentList(studentSearch.value);
      updateSummary();
    });
  });
}

function getValidGuests() {
  return Array.from(guestRows.querySelectorAll(".guest-row"))
    .map(row => ({
      name: row.querySelector(".guest-name").value.trim(),
      phone: row.querySelector(".guest-phone").value.trim()
    }))
    .filter(g => g.name && g.phone);
}

function updateSendState() {
  if (mode === "lateEntry") {
    sendBtn.disabled = !(selected.size > 0 && timeInput.value);
  } else if (mode === "offboarding") {
    sendBtn.disabled = !(leaveInput.value && returnInput.value);
  } else {
    sendBtn.disabled = !(
      visitDateInput.value &&
      visitStartInput.value &&
      visitEndInput.value &&
      getValidGuests().length > 0
    );
  }
}

leaveInput.addEventListener("change", updateSendState);
returnInput.addEventListener("change", updateSendState);
visitDateInput.addEventListener("change", updateSendState);

function addGuestRow() {
  const row = document.createElement("div");
  row.className = "guest-row";
  row.innerHTML = `
    <input type="text" class="guest-name" placeholder="Guest name">
    <input type="text" class="guest-phone" placeholder="Phone number">
    <button type="button" class="guest-remove" aria-label="Remove guest">×</button>
  `;

  row.querySelector(".guest-name").addEventListener("input", updateSendState);
  row.querySelector(".guest-phone").addEventListener("input", updateSendState);
  row.querySelector(".guest-remove").addEventListener("click", () => {
    row.remove();
    updateSendState();
  });

  guestRows.appendChild(row);
}

addGuestBtn.addEventListener("click", addGuestRow);

multiselectToggle.addEventListener("click", () => {
  multiselect.classList.toggle("open");
});

document.addEventListener("click", (e) => {
  if (!multiselect.contains(e.target)) {
    multiselect.classList.remove("open");
  }
});

studentSearch.addEventListener("input", (e) => renderStudentList(e.target.value));

document.getElementById("selectAllBtn").addEventListener("click", () => {
  STUDENTS.forEach(s => selected.add(s.roll_number));
  renderStudentList(studentSearch.value);
  updateSummary();
});

document.getElementById("clearAllBtn").addEventListener("click", () => {
  selected.clear();
  renderStudentList(studentSearch.value);
  updateSummary();
});

function snapToQuarterHour(value) {
  const [hourStr, minuteStr] = value.split(":");
  let hour = Number(hourStr);
  let minute = Math.round(Number(minuteStr) / 15) * 15;

  if (minute === 60) {
    minute = 0;
    hour = (hour + 1) % 24;
  }

  return `${String(hour).padStart(2, "0")}:${String(minute).padStart(2, "0")}`;
}

function bindQuarterHourSnap(input) {
  input.addEventListener("change", () => {
    if (input.value) input.value = snapToQuarterHour(input.value);
    updateSendState();
  });
}

bindQuarterHourSnap(timeInput);
bindQuarterHourSnap(visitStartInput);
bindQuarterHourSnap(visitEndInput);

function fillTemplate(template, values) {
  return Object.entries(values).reduce(
    (text, [key, value]) => text.replace(`{{${key}}}`, value),
    template
  );
}

function buildLateEntryEmail() {
  const tpl = MAIL_TEMPLATES.lateEntry;
  const time = formatTime(timeInput.value);
  const chosen = STUDENTS.filter(s => selected.has(s.roll_number));

  const studentLines = chosen.map((s, i) => `   ${i + 1}. ${s.name} (${s.roll_number}) (${s.course})`).join("\n");

  const vehicleNumber = vehicleInput.value.trim();
  const vehicleLine = vehicleNumber ? `\nVehicle number: ${vehicleNumber}\n` : "";

  const body = fillTemplate(tpl.body, { time, studentLines, vehicleLine });
  const ccList = [tpl.cc, ...chosen.map(s => s.email_id)].join(",");

  return `mailto:${encodeURIComponent(tpl.to)}?cc=${encodeURIComponent(ccList)}&subject=${encodeURIComponent(tpl.subject)}&body=${encodeURIComponent(body)}`;
}

function buildOffboardingEmail() {
  const tpl = MAIL_TEMPLATES.offboarding;
  const leave = formatDateTime(leaveInput.value);
  const back = formatDateTime(returnInput.value);

  const body = fillTemplate(tpl.body, { leave, back });

  return `mailto:${encodeURIComponent(tpl.to)}?cc=${encodeURIComponent(tpl.cc)}&subject=${encodeURIComponent(tpl.subject)}&body=${encodeURIComponent(body)}`;
}

function buildGuestVisitEmail() {
  const tpl = MAIL_TEMPLATES["guest-visit"];
  const visitDate = formatDate(visitDateInput.value);
  const visitStart = formatTime(visitStartInput.value);
  const visitEnd = formatTime(visitEndInput.value);
  const guestLines = getValidGuests().map((g, i) => `   ${i + 1}. ${g.name} (Phone no.: ${g.phone})`).join("\n");

  const body = fillTemplate(tpl.body, { visitDate, visitStart, visitEnd, guestLines });

  return `mailto:${encodeURIComponent(tpl.to)}?cc=${encodeURIComponent(tpl.cc)}&subject=${encodeURIComponent(tpl.subject)}&body=${encodeURIComponent(body)}`;
}

function buildEmail() {
  if (mode === "lateEntry") return buildLateEntryEmail();
  if (mode === "offboarding") return buildOffboardingEmail();
  return buildGuestVisitEmail();
}

sendBtn.addEventListener("click", () => {
  window.location.href = buildEmail();
});

renderStudentList("");
renderSelectedChips();
addGuestRow();
